============
Installation
============

At the command line::

    $ easy_install djangoautoconf

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv djangoautoconf
    $ pip install djangoautoconf